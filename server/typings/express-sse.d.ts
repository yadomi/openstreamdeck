declare module "express-sse";

/* declare module "express-sse" {
  interface Options {
    isSerialized: boolean;
  }

  export default class {
    constructor(initial: Array<any> | object, options?: Options);
    init(): void;
    send(data: object | string, event?: string, id?: string | number);
  }
} */
